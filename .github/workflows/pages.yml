name: Disabled custom Pages workflow

on:
  workflow_dispatch:

jobs:
  notice:
    runs-on: ubuntu-latest
    steps:
      - name: Deployment disabled
        run: |
          echo "Custom GitHub Actions deployment is disabled."
          echo "Use GitHub Pages Settings -> Build and deployment -> Deploy from a branch -> main / root."
